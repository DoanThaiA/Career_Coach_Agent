[project]
name = "thaidq-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.3",
    "langchain>=1.3.2",
    "langchain-text-splitters>=1.1.2",
    "pytest>=9.0.3",
]
